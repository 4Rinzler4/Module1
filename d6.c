#include <stdio.h>
#include <math.h>//підключення бібліотек
int main(){
    int x, y;
    float a;//оголошення змінних
    printf("Введіть значення x:");
    scanf("%d", &x);
    printf("Введіть значення y:");
    scanf("%d", &y);//введення значень
a = ((1+y)/(x+(y/(x*x)+4))/1+(1/(x*x)+4));//обчислення
printf("Результат a: %.2f\n", a);//виведення результату
return 0;//кінець програми
} 